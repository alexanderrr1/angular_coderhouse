import { TamanioFuenteDirective } from './tamanio-fuente.directive';

describe('TamanioFuenteDirective', () => {
  it('should create an instance', () => {
    const directive = new TamanioFuenteDirective();
    expect(directive).toBeTruthy();
  });
});
